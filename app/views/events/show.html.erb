<div class="container">
  <h1>Event title <%= @event.emoji %></h1>
  <!-- To do later: automatic with the name of the emoji but editable -->
  <br>
  <!-- <h5>Toggle button to go from Event Details to Event Messages</h5> -->
  <h5>Proposed by <%= @event.user.first_name %></h5>
  <%= cl_image_tag @event.user.avatar.key, class: "avatar-profile" %>
  <br>
  <br>

  <h5>Where</h5>
  <p><%= @event.address %></p>
  <br>

  <h5>When</h5>
  <p><%= @event.date_time %></p>
  <br>

  <p><%= "Feel free to join with an extra friend!" if @event.open == true %></p>

  <h5>Description</h5>
  <p><%= @event.description %></p>
  <br>

  <%#=  si le mec qui arrive sur la show est host
  j'affiche event parti %>

  <h5>Invitations confirmed</h5>

  <% @event.event_users.each do |event_user| %>
    <p><%= event_user.user_id if event_user.status == "confirmed"  %></p>
  <% end %>

  <% @event.event_users.each do |event_user| %>
    <p><%= event_user.user_id if event_user.status == "pending"  %></p>
  <% end %>

  <% @event.event_users.each do |event_user| %>
    <p><%= event_user.user_id if event_user.status == "declined"  %></p>
  <% end %>

  <% @event.guests.each do |guest|%>
    <p>🙋‍♀️  <%= guest.first_name %></p>
      <%= cl_image_tag guest.avatar.key, class: "avatar-profile" %>
    <p></p>
  <% end %>

  <br>
  <h5>Pending invitations</h5>
  <% @event.guests.each do |guest|%>
    <p>🤷‍♂️  <%= guest.first_name %></p>
      <%= cl_image_tag guest.avatar.key, class: "avatar-profile" %>
    <p></p>
  <% end %>

  <h5>Declined invitations</h5>
  <% @event.guests.each do |guest|%>
    <p>🙋‍♀️  <%= guest.first_name %></p>
      <%= cl_image_tag guest.avatar.key, class: "avatar-profile" %>
    <p></p>
  <% end %>



  <%# if policy(@event).edit? %>
    <%#= link_to "Edit this event", edit_event_path, class: "btn btn-primary" %>
  <%# end %>

  <%# if policy(@event).destroy? %>
    <%#= link_to "Delete #{@event.name}",
                event_path(@event),
                class: "btn btn-primary",
                method: :delete,
                data: { confirm: "Are you sure?" } %>
  <%# end %>

  <%= link_to "Event conversation", @event_path, method: :get %>
  <p><i class="fas fa-paper-plane"></i></p>
  <br>
  <%= link_to "Edit the event", @event_path, method: :get %>
  <br>
  <%= link_to "Invite extra friends", @event_path, method: :get %>
  <br>
  <%= link_to "Share the event", @event_path, method: :get %>
  <br>
  <%= link_to "Delete the event", @event_path, method: :get %>
  <br>
  <br>
  <br>
  <br>

  <!-- if I am host I want to see the edit button -->
  <!-- in any case I want to see the show of the event -->

  </div>
